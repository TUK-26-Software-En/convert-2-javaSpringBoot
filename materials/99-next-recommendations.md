# Next Recommendations

## 목적

- 현재 시점에서 바로 이어서 수행할 수 있는 후속 작업 목록을 저장한다.
- 사용자가 별도 지시할 때까지 참고용 backlog로 유지한다.

## 현재 저장된 추천 목록

1. PlantUML 다이어그램을 `svg` 형식으로도 추가 렌더링
2. `materials/00-index.md`에 각 `png` 다이어그램 링크 추가
3. 제출용 표지/목차를 포함한 통합 문서 형태로 재정리

## 현재 범위 메모

- 별도 추가 요청이 있기 전까지 `service_monitor`에 대한 신규 다이어그램 작성은 진행하지 않는다.
