spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/lib_checkout}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:lib_checkout}
    password: ${DB_PASSWORD:lib_checkout}
